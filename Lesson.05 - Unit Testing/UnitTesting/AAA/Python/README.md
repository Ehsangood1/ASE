# Python Unit Test example

- `python3 -m venv venv`
- `source venv/bin/activate`
- `pip3 install coverage`
- `coverage run --branch test_account.py`
- `coverage html`

HTML coverage pages are stored in the `htmlcov` directory.
